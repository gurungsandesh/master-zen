import styled from "styled-components";

export const NameCircleWrapper = styled.div`
  font-size: 0.7rem;
  letter-spacing: 0.2rem;
  text-transform: uppercase;

  position: fixed;
  top: -2vw;
  left: 2vw;
`;
